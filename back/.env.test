# E2E local: credenciales SOLO para desarrollo (misma DB que docker compose).
# Nunca poner secretos reales aquí. Requiere: docker compose up -d db redis
DB_HOST=localhost
DB_PORT=5432
DB_USER=david
DB_PASSWORD=root
DB_NAME=hotel_db
BCRYPT_ROUNDS=4
JWT_ACCESS_SECRET=test-access-secret
JWT_REFRESH_SECRET=test-refresh-secret
